
  # Internship Tracker Web App

  This is a code bundle for the Internship Tracker Web App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  